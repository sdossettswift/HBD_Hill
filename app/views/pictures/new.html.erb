<%= simple_form_for @picture, url: pictures_path do |f| %>

    <%= f.label :photograph %>
    <%= f.attachment_field :photograph, as: :file %>

  <%= f.input :caption %>
  <%= f.input :source, label: "Your name" %>
  <%= f.button :submit%>
<% end %>
